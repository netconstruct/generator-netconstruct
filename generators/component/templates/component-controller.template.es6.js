/**
 * <%= componentName %> controller.
 * @description The <%= componentName %> controller
 */
class <%= componentClassName %>Controller {
  /* @ngInject */
  constructor($element) {
    this.$element = $element;
  }
}

export default <%= componentClassName %>Controller;
